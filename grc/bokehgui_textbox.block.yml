# auto-generated by grc.converter

id: variable_bokehgui_textbox
label: Bokeh GUI Textbox
category: '[Bokeh GUI]/Bokeh GUI Widgets'
flags:
- show_id

parameters:
-   id: label
    label: Label
    dtype: string
    hide: ${ ('none' if label else 'part') }
-   id: type
    label: Type
    dtype: enum
    default: int
    options: [real, int, string, bool, raw]
    option_labels: [Float, Integer, String, Boolean, Any]
    option_attributes:
        conv: [eng_notation.str_to_num, int, str, bool, eval]
        str: [eng_notation.num_to_str, str, str, str, repr]
    hide: part
-   id: value
    label: Default Value
    dtype: ${ type }
    default: '0'
value: ${ value }

templates:
    imports: |-
        from gnuradio import eng_notation
        import bokehgui
    var_make: self.${id} = ${id} = ${value}
    make: |+
      self.${id}_textbox = bokehgui.textbox(self.widget_lst, str(${value}), ${label if label!="''" else "'{}'".format(id)} +": ")
      self.${id}_textbox.add_callback(
        lambda attr, old, new: self.set_${id}(${type.conv}(new)))

    callbacks:
    - self.set_${id}(${value})
    - self.${id}_textbox.set_value(${value})

documentation: |-
    This block creates a variable with a text entry box. Leave the label blank to use the variable id as the label.

file_format: 1
